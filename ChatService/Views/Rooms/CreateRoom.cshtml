@model ChatService.Model.Room

@{
    ViewData["Title"] = "CreateRoom";
}

<h1>CreateUser</h1>

<h4>User</h4>
<hr />
@using (Html.BeginForm("Create", "RoomController", FormMethod.Post))
    {
        <div class="form-group">
            <label asp-for="Name" class="control-label">Login</label>
            @Html.TextBox("name")
            <input type="submit" value="Create" />
        </div>
    }
    

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
